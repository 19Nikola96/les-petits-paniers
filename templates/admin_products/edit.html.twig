{% extends 'base.html.twig' %}

{% block body %}

<div class="container my-5">

    <h1>Édition Paniers</h1>

    {{ form_start(form) }}

    {{ form_row(form.label) }}
    {{ form_row(form.photo) }}
    {{ form_row(form.description) }}
    {{ form_row(form.price) }}

    <div class="text-right">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('admin_products') }}" class="btn btn-outline-secondary">Retour</a>
    </div>


    {{ form_end(form) }}
</div>



{% endblock %}

